Install scripts for BridgeBox on the Raspberry PI

To initialise a new PI:

```
curl -sSL https://raw.githubusercontent.com/rhindonltd/bridge-box/refs/heads/main/install.sh | bash -x 2>&1 | tee ~/install.log
```